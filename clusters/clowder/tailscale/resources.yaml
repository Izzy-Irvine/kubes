---
apiVersion: v1
kind: Namespace
metadata:
  name: tailscale
  # The proxy pods for Service-type (L3) exposure need a privileged
  # init container to set sysctls
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/audit-version: latest
    pod-security.kubernetes.io/warn: privileged
    pod-security.kubernetes.io/warn-version: latest
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: tailscale
  namespace: tailscale
spec:
  interval: 1m0s
  url: https://pkgs.tailscale.com/helmcharts
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  chart:
    spec:
      chart: tailscale-operator
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: tailscale
      version: 1.98.9
  # OAuth credentials come from the operator-oauth secret
  # (operator-oauth.k8s-sops.yaml), not from chart values
  interval: 5m0s
---
# Exit node: route device traffic through home. Needs "autoApprovers":
# {"exitNode": ["tag:k8s"]} in the tailnet ACL (or one-off approval).
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: exit-node
spec:
  hostnamePrefix: exit-home
  exitNode: true
