apiVersion: v1
kind: Service
metadata:
  namespace: pwn2own-ghidra
  name: pwn2own-ghidra
  labels:
    app: pwn2own-ghidra
spec:
  ports:
    - port: 13100
      targetPort: 13100
      nodePort: 30690
      protocol: TCP
  type: NodePort
  selector:
    app: pwn2own-ghidra
