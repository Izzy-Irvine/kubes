apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: red-ping-api
  namespace: red-ping
spec:
  imageRepositoryRef:
    name: red-ping-api
  policy:
    numerical:
      order: asc
